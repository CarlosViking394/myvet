# Temporarily suppress audit warnings during development
audit=false

# Continue to allow production builds to fail on vulnerabilities
# This ensures production deployments remain secure
production-audit=true 